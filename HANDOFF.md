# Root Handoff Pointer

`wf-harness/HANDOFF.md` is the authoritative handoff path for the current Wealth Factory build.

Phase R1 of the post-audit realignment retired the duplicate repo-root build-status narrative on purpose so there is one current handoff lane instead of two drifting summaries.

Use this file only as a pointer when older docs or recovery notes still mention `HANDOFF.md`.
