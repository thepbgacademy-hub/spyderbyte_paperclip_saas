server {
  listen 80;
  server_name www.spyderbyte.cloud api.spyderbyte.cloud;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl http2;
  server_name www.spyderbyte.cloud;

  ssl_certificate /etc/letsencrypt/live/www.spyderbyte.cloud/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/www.spyderbyte.cloud/privkey.pem;

  add_header X-Content-Type-Options nosniff always;
  add_header Referrer-Policy same-origin always;
  add_header X-Frame-Options DENY always;

  location / {
    proxy_pass http://web:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}

server {
  listen 443 ssl http2;
  server_name api.spyderbyte.cloud;

  ssl_certificate /etc/letsencrypt/live/api.spyderbyte.cloud/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.spyderbyte.cloud/privkey.pem;

  add_header X-Content-Type-Options nosniff always;
  add_header Referrer-Policy same-origin always;
  add_header X-Frame-Options DENY always;

  location / {
    proxy_pass http://api:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
