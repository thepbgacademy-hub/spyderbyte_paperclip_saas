# Wealth Factory Harness Phase 1 Security Report

Date: 2026-05-21
Scope: First custom harness slice under `src/harness`, `src/api/harness-http.ts`, `src/api/runtime-server.ts`, `src/config/env.ts`, `src/wealthfactory/workflow-registry.ts`, `apps/web/src/pages/HarnessBoardPage.tsx`, `apps/web/src/components/HarnessBoard.tsx`, `apps/web/src/components/HarnessCardDrawer.tsx`, `apps/web/src/harness-board-client.ts`, and related tests.

## Executive Summary

- Confirmed findings: `0 Critical`, `0 High`, `0 Medium`, `2 Low`, `2 Info`
- Overall risk score: `4` (`Low`)
- Secret exposure status: no confirmed live secrets in the tracked harness slice or targeted git-history scan
- Top immediate actions:
  1. Keep root `.env` and other local operational artifacts excluded from staging and deployment bundles.
  2. Keep the new harness runtime contract sanitized so only `providerKind` and `credentialLabel` persist in `runtime_context`.
  3. Preserve response-guard checks and guarded 401/500 separation on the harness board API so customer-facing payloads cannot leak prompt/tool/secret fields or mask internal failures as auth issues.

## Methodology

- Refreshed GitNexus index, then used it as a preflight map for the new harness/runtime/board seams.
- Inventoried current secret surfaces with `rg --files` for env/config/key-like files.
- Ran a broad keyword scan across `src`, `apps/web/src`, `tests`, and `wf-harness`.
- Narrowed the scan to the changed harness slice with provider-pattern checks for live-looking secret formats.
- Scanned targeted git history for secret-shaped values and secret-boundary env names in the touched slice.
- Reviewed the customer-facing response guard and the new runtime-context sanitization path.
- Re-ran the scan after wiring the persisted board service, repository-backed run/card/event storage, and browser fetch fallback.
- Re-ran the scan again after adding credentialed board CORS support and atomic seed protection; no new secret-exposure findings were introduced.
- Re-ran the scan after separating tenant/package access denials from infrastructure failures and restricting the browser fallback board to loopback-only hosts; no new secret-exposure findings were introduced.
- Re-ran the scan after adding persisted sub-card proposals and the guarded CEO approval mutation path; the only matches in the touched slice were expected auth header names, dummy `Bearer valid` test values, and the intentional runtime-context sanitization test fixture.
- Re-ran the scan after the reviewer-fix pass for stale CEO gate reset and idempotent proposal approval. The only matches remained expected auth header names, dummy test placeholders, and the intentional `secretValues` sanitization fixture; no live secret material was introduced.
- Re-ran the scan after hardening proposal approval to require an atomic runner and after deferring the approval-card foreign key for transaction-safe persistence. The result stayed clean: only expected auth header names, dummy placeholders, and the sanitization fixture matched.
- Re-ran the scan after adding the guarded `POST /api/harness/cards` direct-child mutation and shrinking bootstrap seeding to CEO only. The result stayed clean: no live secret material was introduced, and the narrowed write seam still exposes only expected auth names, dummy placeholders, and the intentional sanitization fixture.
- Re-ran the scan after making denied governance explicitly exportable through the existing governance-history and package-bundle candidates. The result stayed clean: the contract widened tenant-facing record truth, but it did not add a new denial-only export surface or widen secret-bearing runtime state into board/export payloads.
- Re-ran the scan after hardening the direct-child mutation for retry idempotency, open-card limits, and non-nested atomic seeding. The result stayed clean: only expected auth names, dummy placeholders, and the intentional sanitization fixture remained in scope.
- Re-ran the scan after adding transaction-client coverage for the deferred proposal-approval seam. The result stayed clean: the touched slice only exposed expected auth names, dummy placeholders, and the existing sanitization fixture; no live secret material was introduced.
- Re-ran the scan after landing the real disposable-Postgres proof path plus the guarded child-card progression/result-recording seam. The touched slice still only exposed expected auth names, dummy placeholders, local disposable test credentials for the isolated Docker database, and the existing sanitization fixture; no live secret material was introduced.
- Re-ran the scan after rejecting invalid child-card states at the HTTP seam, removing query-string outcome summaries from the public advancement route, and tightening the docs around Docker-gated proof coverage. The result stayed clean: no live secret material was introduced.
- Re-ran the scan after adding run-level reconciliation plus metadata-only harness audit publishing for child-card creation, proposal approval, and advancement. The touched slice still only exposed expected auth names, dummy placeholders, and the existing sanitization fixture; no live secret material or raw outcome text was introduced.
- Re-ran the scan after hardening post-commit harness audit publishing so audit-sink outages log a warning instead of making already-committed mutations appear failed. The touched slice still introduced no live secret material and still keeps raw outcome text out of durable audit payloads.
- Re-ran the scan after moving harness mutations to parsed JSON bodies, adding the explicit CEO completion command, and tightening duplicate-lane policy. The touched slice still introduced no live secret material, and tenant-authored summaries now stay off URL surfaces while remaining excluded from durable audit payloads.
- Re-ran the scan after hardening the Node adapter to enforce request-size limits on bytes actually read, splitting dashboard `401` vs `500` outcomes, and making the touched runtime-server test fixture use the clearly fake `db.invalid` host plus a placeholder password. The touched slice still introduced no live secret material.
- Re-ran the scan after closing the cross-tenant approved-proposal leak, bounding child-card persona/deliverable inputs to the approved catalog, and making storage OAuth degrade per provider with structured unavailable responses. The touched slice still introduced no live secret material, and no new secret-bearing response paths were added.
- Re-ran the scan after binding storage OAuth callback completion to the route provider, validating `WF_STORAGE_OAUTH_REDIRECT_ORIGIN` against the allowed portal-origin set, and tightening harness route classification so dead proposal paths no longer consume the live approval rate-limit bucket. The touched slice still introduced no live secret material and did not add any new secret-bearing response paths.
- Re-ran the scan after normalizing the accepted storage OAuth redirect origin and preserving pending OAuth state on provider-mismatch callback attempts. The touched slice still introduced no live secret material, and the callback integrity seam now fails closed without consuming the tenant's one valid retry path.
- Re-ran the scan after widening harness proposal policy to support persisted `defer`/`deny` outcomes, lane-reuse decisions, and the derived `completionPackage` read model. The touched slice still introduced no live secret material, and the new decision notes remain bounded business text rather than secret-bearing config.
- Re-ran the scan after wiring migration-helper support for `0015_wf_harness_proposal_resolutions.sql` and updating the disposable Postgres proof path. The touched slice still introduced no live secret material, and the new Docker-backed test path continues using isolated local-only credentials.
- Re-ran the scan after making deferred proposals visible/re-approvable, restoring a parent-card approval trail for reused lanes, and tightening `/approve` to reject non-approved terminal outcomes. The touched slice still introduced no live secret material and did not widen any tenant secret or audit payload surface.
- Re-ran the scan after adding the append-only harness board-decision ledger, the bounded `recentDecisions` read model, and auto-defer behavior for cross-persona deliverable-owner conflicts. The touched slice still introduced no live secret material, and the new decision ledger stores only bounded workflow metadata plus optional business-facing decision notes.
- Re-ran the scan after the reviewer pass tightened board-decision semantics: the migration helper now re-checks the `0016` schema after apply, `recentDecisions` no longer echoes raw `decisionNote` text, and the CEO completion summary is no longer duplicated into the decision ledger. The touched slice still introduced no live secret material.
- Re-ran the scan after widening the board-decision ledger with bounded policy reasons plus recommendation/objection summaries, and after teaching deferred approvals plus `completionPackage` to surface governance caveats from that ledger. The touched slice still introduced no live secret material and still kept raw CEO notes out of the public board feed.
- Re-ran the scan after tightening the migration-helper constraint check and trimming stale governance history out of `completionPackage`. The final touched slice still introduced no live secret material; only expected auth placeholders and the isolated Docker test DSN remained in scope.
- Re-ran the scan after making repeated defer decisions idempotent and widening `completionPackage` into structured governance items for deferred/denied requests. The touched slice still introduced no live secret material and still kept raw `decisionNote` text out of the public tenant-facing package.
- Re-ran the scan after fixing reviewer-found packaging edge cases so governance summaries now derive from the full decision set and denied-only packages still keep governance content visible. The touched slice still introduced no live secret material and still kept raw `decisionNote` text out of the public tenant-facing package.
- Re-ran the scan after adding structured `proposal_absorbed` lane events for reused approvals and after tightening the local Docker-daemon readiness check for the disposable Postgres proof path. The touched slice still introduced no live secret material and still kept absorbed-work board history free of raw secret-bearing payloads.
- Re-ran the scan after redacting raw `decisionNote` text from the tenant-facing lane activity feed and after correcting all-cancelled child lanes to derive `blocked` instead of a misleading `active`. The touched slice still introduced no live secret material and further reduced the risk of leaking free-form internal business notes through public board activity.
- Re-ran the scan after making denied public board messages policy-aware again for owner-conflict and lane-cap cases. The touched slice still introduced no live secret material, and the narrower public activity feed now stays both bounded and truthful without replaying raw CEO notes.
- Re-ran the scan after adding the bounded owner-conflict lane-handoff seam, widening harness resolution checks with `handoff_existing_lane`, and accepting a guarded `targetCardId` on proposal approval routes. The touched slice still introduced no live secret material and only reassigns persisted lane metadata without widening the BYOK or runtime-secret boundary.
- Re-ran the scan after surfacing bounded handoff-target hints in deferred owner-conflict approvals and making stale `targetCardId` hints fall back to normal governance instead of hard-failing a valid approval path. The touched slice still introduced no live secret material and did not widen any credential or runtime-secret boundary.
- Re-ran the scan after making `assembling`/`done` runs fail closed on both follow-on proposal approvals and direct CEO child-lane creation under `completed_lanes_only`, and after making terminal runs read-only at the child-card seam instead of relying only on reconciliation. The touched slice still introduced no live secret material and did not widen any credential-bearing persistence or response surface.
- Re-ran the scan after hardening harness export delivery with stale-claim recovery, governance-history-before-package dependency enforcement, compare-and-set delivery outcomes, and bounded partial-writer receipts. The touched slice still introduced no live secret material; partial failure state stores only counts plus last-attempted relative paths, and package bodies remain confined to the private writer seam plus harness-owned delivery ledger.
- Re-ran the scan after adding derived `followThroughItems` from the persisted decision ledger so the board can show implemented governance history without replaying raw `decisionNote` text or card chatter. The touched slice still introduced no live secret material and kept implemented history bounded to existing safe decision fields.
- Re-ran the scan after adding the explicit `startFreshCycle` seam plus fresh-cycle carry-forward filtering for `completed_lanes_only` deferred proposals. The touched slice still introduced no live secret material and reopened follow-on work only by cloning bounded proposal metadata into a new run, not by widening runtime-context or credential persistence.
- Re-ran the scan after widening `startFreshCycle` with an explicit clean-vs-reopen mode and JSON-body parsing at the HTTP seam. The touched slice still introduced no live secret material and still keeps fresh-cycle inputs bounded to run id plus a small policy mode, with no BYOK, runtime-context, or tenant-authored business text widening.
- Re-ran the scan after tightening repeated unresolved proposal governance so newer retries of the same request now defer or deny against the latest earlier unresolved governance state instead of opening fresh lanes, while active-lane reuse and owner-conflict handoff still win first. The touched slice still introduced no live secret material and still keeps repeated-request handling bounded to proposal metadata, policy labels, and internal decision notes rather than any credential-bearing state.
- Re-ran the scan after the reviewer-fix pass narrowed repeated-request matching to the same request title/persona/deliverable triple and changed the helper to look at the latest earlier unresolved request instead of the first one. The touched slice still introduced no live secret material and still keeps the repeated-request guard entirely inside bounded governance metadata.
- Re-ran the scan after adding the bounded `harness_card_continuity` seam for live lane continuity snapshots. The touched slice still introduced no live secret material and still keeps continuity persistence limited to operational lane summaries, latest outcomes, and absorbed-work labels rather than any BYOK or runtime-secret state.
- Re-ran the scan after the continuity reviewer fix pass tightened handoff attribution and deterministic absorbed-work merge behavior. The touched slice still introduced no live secret material, and the continuity seam still stores only policy-bounded operational board labels instead of any provider or tenant secret values.
- Re-ran the scan after promoting `continuitySummary` into the real per-lane resume directive and threading continuity through runtime resume. The touched slice still introduced no live secret material and still keeps continuity limited to bounded operational board text rather than any provider credential, BYOK secret, or raw tenant note payload.
- Re-ran the scan after hardening the done-lane seam so `resumeSummary` is rejected on terminal `done` transitions. The touched slice still introduced no live secret material and further reduced the risk of stale operational board text overriding a completed-lane snapshot.
- Re-ran the scan after adding the first worker-side harness lane-dispatch seam for `wf_harness_v1` workflows. The touched slice still introduced no live secret material and keeps the dispatch payload bounded to persisted lane metadata plus `resumeFocus` / latest outcome text, without widening BYOK, provider secrets, or tenant-authored internal notes.
- Re-ran the scan after adding the worker-private lane outcome commit seam plus compare-and-set child-lane transitions. The touched slice still introduced no live secret material and keeps worker-written outcomes bounded to lane state, continuity text, and optional result summaries rather than any BYOK, provider, or tenant-secret payload.
- Re-ran the scan after deepening the worker-private seam into bounded follow-on dispatch. The touched slice still introduced no live secret material and keeps the new follow-on payload limited to persona/lane metadata, continuity-backed resume focus, and optional latest-result summaries instead of any BYOK, provider binding, or tenant-secret fields.
- Re-ran the scan after wiring guarded workflow-status recording behind the bounded follow-on dispatch seam. The touched slice still introduced no live secret material and only propagates existing run/workflow ids plus a fixed `running` status through the private worker path, without widening any tenant-authored or credential-bearing surface.
- Re-ran the scan after adding the bounded completed-lane refinement reopen seam. The touched slice still introduced no live secret material and only reuses existing lane ids, state transitions, continuity text, and latest-result summaries instead of widening any tenant-authored secret, BYOK, or provider-binding surface.
- Re-ran the scan after extending the bounded completed-lane refinement reopen rule to direct CEO-created child lanes. The touched slice still introduced no live secret material and only reuses existing lane ids, state transitions, continuity text, and latest-result summaries instead of widening any tenant-authored secret, BYOK, or provider-binding surface.
- Re-ran the scan after normalizing dynamic lane-title matching for minor wording variants across reuse, reopen, and unresolved-request dedupe paths. The touched slice still introduced no live secret material and only compares bounded title text plus existing lane metadata instead of widening any secret-bearing surface.
- Re-ran the scan after letting direct CEO follow-on requests fold into an already open same-persona/same-deliverable lane with bounded continuity updates. The touched slice still introduced no live secret material and only appends existing lane metadata plus bounded continuity text instead of widening any credential-bearing or BYOK surface.
- Re-ran the scan after deriving worker-recorded workflow status from committed harness lane outcomes when no immediate follow-on dispatch exists. The touched slice still introduced no live secret material and only maps existing run-state metadata into queue-safe status values instead of widening any credential-bearing or BYOK surface.
- Re-ran the scan after fixing the Postgres continuity upsert so omitted follow-up writes preserve the prior `latestResultSummary` instead of nulling it. The touched slice still introduced no live secret material and only tightened persistence parity between in-memory and real Postgres harness state.
- Re-ran the scan after adding direct CEO lane-cap deferral, the bounded `continuitySource` discriminator, and the new private claimed-lane execution envelope. The touched slice still introduced no live secret material; runtime context remains sanitized, public dispatch telemetry still excludes provider context and required capabilities, and the new continuity metadata stores only bounded operational state.
- Re-ran the scan after deepening the private claimed-lane envelope with parent-card identity, continuity-source metadata, absorbed-work items, and explicit worker outcome rules. The touched slice still introduced no live secret material; the richer worker context remains private to the hook seam, runtime context stays sanitized, and public lane-dispatch telemetry still excludes execution-only payload like required capabilities, outcome rules, and continuity items.
- Re-ran the scan after adding the worker post-outcome orchestrator decision seam. The touched slice still introduced no live secret material and keeps the new `postOutcomeAction` payload bounded to non-secret control metadata (`kind`, `runState`, bounded `reason`, and lane ids/personas when needed) instead of widening BYOK, provider context, or tenant-secret surfaces.
- Re-ran the scan after preserving structured harness HTTP failure codes in the browser client and widening the board page to render `resolve-attention` contract guidance directly. The touched slice still introduced no live secret material; the new client/page behavior only carries bounded error codes, retry metadata, and existing tenant-safe contract text instead of any BYOK, provider, or secret-bearing payloads.
- Re-ran the scan after adding explicit loopback preview variants for `review-attention` and `resolve-attention`, and after surfacing the active preview variant label in the board page. The touched slice still introduced no live secret material; the new preview selector carries only bounded fallback contract choice and tenant-safe action metadata, not any provider context, BYOK material, or secret-bearing state.
- Re-ran the scan after widening the loopback preview selector to cover the governance-backlog `pending-approvals` attention branch too. The touched slice still introduced no live secret material; the new preview branch carries only existing tenant-safe queue metadata and bounded attention labels, not any provider context, BYOK material, or secret-bearing state.
- Re-ran the scan after adding bounded first-target metadata to governance-backlog attention and mirroring that through the preview branch and board pulse. The touched slice still introduced no live secret material; the new queue-target labels are derived only from existing tenant-safe proposal metadata and do not widen provider context, BYOK material, or secret-bearing state.

## OWASP-Oriented Findings

### A01 Broken Access Control

No issues identified in the harness slice. The new board HTTP surface preserves origin checks, preflight handling, and request authentication input flow instead of exposing an unauthenticated public board endpoint.

### A02 Security Misconfiguration

#### [LOW] Local operational env/config surfaces still exist at the repo root

- Paths:
  - `E:\REPOS\spyderbyte_paperclip_saas\.env`
  - `E:\REPOS\spyderbyte_paperclip_saas\deploy\docker-compose.yml`
- Rationale: the harness slice itself does not commit secrets, but root operational surfaces remain present locally and could be staged accidentally if repo hygiene regresses.
- Evidence: inventory scan found `.env` plus deployment manifests; no live values were echoed into this report.
- Recommendation: keep `.env` untracked, keep local-only files in ignore/exclude rules, and keep deployment packaging restricted to explicit allowlists.

### A03 Software Supply Chain Failures

No issues identified in the harness slice.

### A04 Cryptographic Failures

No issues identified in the harness slice. The new runtime contract and migration explicitly reject persisted `secretValues`, which reduces accidental plaintext persistence.

### A05 Injection

No issues identified in the harness slice.

### A06 Insecure Design

No issues identified in the harness slice. The new board route is read-only and high-level; it does not expose raw orchestration inputs or sub-agent mechanics.

### A07 Authentication Failures

No issues identified in the harness slice. The new board handler reuses the guarded request shape with authorization/cookie passthrough rather than introducing a parallel trust model.

### A07/A10 Storage OAuth Callback Integrity

No confirmed issue remains in the current slice. The callback handler now requires the public route provider to match the pending OAuth state before token exchange begins, so a miswired callback path fails closed instead of silently finishing the wrong provider flow.

### A08 Software or Data Integrity Failures

No issues identified in the harness slice.

### A09 Security Logging and Alerting Failures

#### [INFO] Harness audit publishing is now metadata-only and best-effort, but sink outages can still drop audit rows after a successful mutation

- Paths:
  - `E:\REPOS\spyderbyte_paperclip_saas\src\harness\board-service.ts`
  - `E:\REPOS\spyderbyte_paperclip_saas\src\api\runtime-server.ts`
- Rationale: the harness now emits explicit audit events for the current card/proposal/run reconciliation mutations, and audit outages no longer make committed mutations look like failures. But because publishing happens after commit, a sink outage still means the business mutation succeeds while the durable audit row is skipped.
- Recommendation: keep this best-effort path for the current bounded slice, but move audit publishing inside a stronger shared persistence or outbox seam before claiming full guaranteed audit durability.

### A10 Mishandling of Exceptional Conditions

No issues identified in the harness slice. The board HTTP boundary now distinguishes guarded 401 auth failures from internal 500 conditions instead of collapsing everything into `unauthorized`.

## Secret Exposure Scan

### Confirmed Results

- No confirmed live secrets were found in the tracked harness slice or the targeted history scan.
- The narrowed provider-pattern scan matched only env-variable names, dummy test values, and authorization placeholders used in tests.
- The new harness persistence layer strips `secretValues` before runtime context is stored or serialized, and the repository-backed board path continues using only sanitized runtime context fields.
- The guarded harness mutation routes now accept tenant-authored summaries through parsed JSON bodies instead of URL query strings, which keeps business text out of browser history and intermediary URL logs.
- The widened proposal policy and new board-decision ledger store only bounded workflow metadata (`status`, `resolution`, optional `decisionNote`) and do not introduce any new credential-bearing persistence paths. The public `recentDecisions` feed now renders bounded summaries instead of replaying raw decision-note text.
- The new reused-lane `proposal_absorbed` event stores only bounded workflow metadata (`proposalId`, `parentCardId`, `requestedByPersona`, `requestedTitle`, `deliverableType`, `resolution`) and does not widen the secret boundary or persist provider material.
- The public board activity feed now emits bounded governance status messages for proposal decisions instead of replaying raw `decisionNote` text, which lowers the risk of leaking internal business notes or accidental sensitive phrasing into the tenant-facing surface.
- The derived `completionPackage` board view is assembled from existing persisted CEO and child-card outcomes and does not persist raw provider secrets or expand the runtime-context trust boundary.
- The new `harness_card_continuity` persistence path stores only policy-bounded operational board text (`continuitySummary`, `latestResultSummary`, `absorbedWorkItems`) and does not expand the runtime-context trust boundary or duplicate any provider credential state.
- The new `governanceItems` completion-package view is derived from bounded ledger fields (`policyReason`, `recommendationSummary`, `objectionSummary`) and intentionally excludes raw `decisionNote` text, which keeps ad hoc CEO notes out of the public board handoff.
- The worker-side harness lane-dispatch seam now emits only bounded lane metadata plus continuity-backed resume focus for the next actionable non-CEO lane, and terminal/no-actionable-lane cases stay quiet instead of widening execution noise or secret exposure.
- The worker-side harness claim/start seam now requires a durable `approved -> working` compare-and-set before any lane dispatch is emitted or any harness run is marked `running`, which keeps false progress and stale-lane races out of the public worker surface.
- Raw `queued` child lanes remain intentionally non-executable in the worker seam, which preserves the CEO approval boundary and avoids widening execution behavior into unreviewed staged work.
- The tightened worker-start seam now persists only bounded state/event/continuity updates around that claim (`state_changed`, active-lane `continuitySummary`, preserved `latestResultSummary`), without widening BYOK, provider secrets, or raw tenant notes into the worker surface.
- The new worker-private lane outcome seam stays inside the same bounded trust model: it accepts only a claimed `working` lane, records only `state_changed` plus optional bounded `result_recorded` summaries, refreshes continuity without exposing BYOK or provider material, and emits a private worker event instead of reopening the tenant-facing board API.
- Child-lane advancement now uses compare-and-set state transitions in both the public and private seams, which reduces the risk of stale workers or duplicate writes silently overwriting a lane after its state already changed elsewhere.
- Worker outcomes now carry a bounded `postOutcomeAction` control contract in addition to any optional follow-on dispatch, and that contract is limited to orchestration metadata (`dispatch_next_lane`, `queue_ceo_review`, `await_lane_resume`, `await_unblock`) rather than any credential-bearing or tenant-secret payload.
- The new attention-state seam also stays bounded: `attention_requested` and `attention_resolved` record only control metadata plus lane ids/personas and optional continuity-backed labels, and the board `pendingAttention` view does not widen provider context, required capabilities, or BYOK-bearing runtime state.

### Additional Hygiene Observations

#### [LOW] Root operational proof artifacts should stay out of customer bundles

- Paths:
  - `E:\REPOS\spyderbyte_paperclip_saas\tmp_stage_*.json`
  - `E:\REPOS\spyderbyte_paperclip_saas\tertiary_runs.json`
  - `E:\REPOS\spyderbyte_paperclip_saas\audit\2026-05-20\*.json`
  - `E:\REPOS\spyderbyte_paperclip_saas\audit\2026-05-21\*.json`
- Rationale: these are not confirmed secret leaks, but they are operational artifacts that should never be treated as customer-facing deployment assets.
- Recommendation: keep deployment packaging allowlisted and avoid copying repo-root artifacts into runtime images or release bundles.

#### [INFO] Test fixtures intentionally use dummy secret-shaped placeholders

- Paths:
  - `E:\REPOS\spyderbyte_paperclip_saas\tests\env.test.ts`
  - `E:\REPOS\spyderbyte_paperclip_saas\tests\runtime-server.test.ts`
  - `E:\REPOS\spyderbyte_paperclip_saas\tests\harness-http.test.ts`
- Evidence: examples include masked/dummy values like `service-role-key`, `paperclip-service-token`, `Bearer valid`, and the clearly fake `postgresql://postgres.tenant:***@db.invalid:5432/postgres` test fixture.
- Recommendation: keep using clearly fake placeholders and continue avoiding live provider-shaped values in tests.

- Re-ran the scan after fixing three reviewer-found worker/governance seams: follow-on worker dispatches now also emit the private execution envelope, direct CEO lane-cap deferrals now audit `hasDecisionNote: true`, and already-`working` lane redispatches no longer fabricate `working -> working` history or flatten richer continuity sources. The touched slice still introduced no live secret exposure, and public lane-dispatch telemetry remains metadata-only without leaking required capabilities or runtime context.
- Re-ran the scan after consuming non-dispatch `postOutcomeAction` results inside the worker/runtime handoff. The touched slice still introduced no live secret exposure; the new `wealth_factory_harness_post_outcome_action` event and optional runtime hook carry only bounded control metadata plus the already-sanitized lane execution snapshot, without widening provider context, required capabilities, or BYOK-bearing state.
- Re-ran the scan after adding explicit runtime-side handlers for `queue_ceo_review`, `await_lane_resume`, and `await_unblock`. The touched slice still introduced no live secret exposure; the new hooks only partition the same bounded non-dispatch handoff payload and do not widen telemetry or provider-context surfaces.
- Re-ran the scan after isolating generic and specific post-outcome hook failures from each other. The touched slice still introduced no live secret exposure; the new warning path only logs bounded run/workflow/card metadata plus the action kind, and it does not widen provider context, required capabilities, or BYOK-bearing state.
- Re-ran the scan after adding dedicated worker events for CEO review, lane resume, and lane unblock. The touched slice still introduced no live secret exposure; the new events mirror the existing bounded non-dispatch payload and add no provider context, required capabilities, or BYOK-bearing state.
- Re-ran the scan after extracting a shared post-outcome classifier, persisting bounded `attention_requested` events, and surfacing `pendingAttention` through the board read model. The touched slice still introduced no live secret exposure; the new shared seam only carries bounded control metadata and continuity-backed summaries, while provider context, required capabilities, and BYOK-bearing runtime data remain outside the public board payload.
- Re-ran the scan after making attention state durable and non-spammy: repeated unresolved attention now stays idempotent, later durable lane progress persists bounded `attention_resolved` events, and the board clears stale `pendingAttention` only when the earlier attention need is durably resolved. The touched slice still introduced no live secret exposure; the new attention-state events carry only bounded action metadata plus lane ids/personas and optional continuity-backed labels.
- Re-ran the scan after adding explicit worker `attentionTransition` metadata, suppressing unchanged-attention runtime chatter, and emitting the dedicated bounded runtime `attention_resolved` handoff/event. The touched slice still introduced no live secret exposure; the new runtime signals only partition already-sanitized control metadata and do not widen provider context, required capabilities, or BYOK-bearing state.
- Re-ran the scan after persisting bounded attention snapshot metadata on `attention_requested` / `attention_resolved` events and teaching board reads to prefer those persisted snapshots in `pendingAttention` and attention activity history. The touched slice still introduced no live secret exposure; the stored snapshots carry only bounded labels/targets that were already tenant-safe enough for the board and still exclude provider context, required capabilities, and secret-bearing runtime data.
- Re-ran the scan after adding the explicit CEO `reviewPendingAttention` mutation and its guarded `/api/harness/runs/:runId/review-attention` HTTP seam. The touched slice still introduced no live secret exposure; the new route accepts only bounded control decisions (`complete_run` or `start_fresh_cycle`) plus optional tenant-authored completion text in the request body, and it does not widen provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after adding the explicit board-side `resolvePendingAttention` mutation and its guarded `/api/harness/runs/:runId/resolve-attention` HTTP seam. The touched slice still introduced no live secret exposure; the new route accepts only bounded control commands (`resume_lane` or `unblock_lane`) plus optional tenant-authored resume text in the request body, and it does not widen provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after wiring resolved harness attention back into the existing workflow queue seam. The touched slice still introduced no live secret exposure; the new board-service hook and runtime-server requeue path carry only bounded tenant/run/workflow/card metadata plus the already-approved command kind, and they do not widen provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after wiring fresh-cycle creation back into the existing workflow queue seam. The touched slice still introduced no live secret exposure; the new board-service hook and runtime-server requeue path carry only bounded tenant/run/workflow metadata plus the approved fresh-cycle mode/count, and they do not widen provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after making explicit CEO review resolution persist bounded `attention_resolved` history through both `complete_run` and `start_fresh_cycle`. The touched slice still introduced no live secret exposure; the new persisted resolution payload carries only bounded orchestration metadata plus the same tenant-safe review labels already used in board attention views, without widening provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after making `pendingAttention` reason-aware for `queue_ceo_review`. The touched slice still introduced no live secret exposure; the board now withholds explicit review commands outside the real final-assembly seam, and the change only narrows control metadata without widening provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after routing governance-only CEO attention toward a bounded `pending-approvals` surface. The touched slice still introduced no live secret exposure; the new action route and approval count expose only existing tenant-safe proposal-queue metadata and do not widen provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after exposing bounded `proposal-decision` action metadata on each `pendingApprovals` row. The touched slice still introduced no live secret exposure; the new row-level action contract carries only finite control metadata (`approve`, `defer`, `deny`) and does not widen provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after exposing bounded `actionPath` metadata for proposal decisions and actionable attention states. The touched slice still introduced no live secret exposure; the new paths point only at existing guarded harness endpoints and do not carry provider context, required capabilities, BYOK material, or tenant-authored business content.
- Re-ran the scan after exposing bounded `actionMethod` plus minimal `requestFields` metadata for proposal decisions and actionable attention states. The touched slice still introduced no live secret exposure; the new metadata carries only finite control-field descriptions and optional suggested handoff ids, and it does not widen provider context, required capabilities, BYOK material, or raw tenant-authored business content.
- Re-ran the scan after exposing bounded action labels/descriptions and per-option choice metadata for proposal decisions and actionable attention states. The touched slice still introduced no live secret exposure; the new metadata carries only finite control semantics for already-guarded actions and does not widen provider context, required capabilities, BYOK material, or tenant-authored business content beyond bounded summaries already permitted on the board.
- Re-ran the scan after exposing bounded option-level example payloads, emphasis metadata, and target summaries for proposal decisions and actionable attention states. The touched slice still introduced no live secret exposure; the new metadata carries only finite control examples and tenant-safe lane summaries, and it does not widen provider context, required capabilities, BYOK material, or raw tenant-authored business content.
- Re-ran the scan after exposing bounded recommended-option, next-effect, and confirmation metadata for proposal decisions and actionable attention states. The touched slice still introduced no live secret exposure; the new metadata carries only finite control guidance for already-guarded actions and does not widen provider context, required capabilities, BYOK material, or raw tenant-authored business content.
- Re-ran the scan after teaching the localhost fallback and board page to consume the richer harness action contract. The touched slice still introduced no live secret exposure; the new fallback/page rendering uses only bounded board metadata already approved for tenant display and does not widen provider context, required capabilities, BYOK material, or raw tenant-authored business content.
- Re-ran the scan after teaching the board page and fallback to render bounded field constraints and attention context from the same harness contract. The touched slice still introduced no live secret exposure; the new page copy uses only tenant-safe control metadata such as required/optional labels, allowed values, suggested values, and attention timing/reason, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the board page to render bounded option emphasis, allowed decision/command sets, and per-option example payloads directly from the harness contract. The touched slice still introduced no live secret exposure; the new page copy uses only tenant-safe control metadata already present in the board contract and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the board page and fallback to render bounded approval governance metadata such as requested-by context, policy reasons, next-review triggers, last-decision timing, and action-family labels. The touched slice still introduced no live secret exposure; the new page copy uses only tenant-safe governance metadata already present in the board contract and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the board page and fallback to render bounded governance history and package read models (`recentDecisions`, `followThroughItems`, and `completionPackage`). The touched slice still introduced no live secret exposure; the new page copy uses only tenant-safe board history and packaged-outcome metadata already present in the contract and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the board page to surface governance/package counts and humanized labels from the same board contract. The touched slice still introduced no live secret exposure; the new metrics and count labels use only tenant-safe metadata already present in the contract and do not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the board page to group contract-driven attention, approvals, and package posture into a bounded board-pulse summary. The touched slice still introduced no live secret exposure; the new pulse copy only re-groups tenant-safe metadata already present in the board contract and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the live board page to submit bounded proposal/review actions through engine-supplied action paths and example payloads. The touched slice still introduced no live secret exposure; the new client/page mutation seam posts only finite control payloads already described by the contract, and localhost fallback remains read-only instead of attempting fake live mutations.
- Re-ran the scan after teaching the board page to reflect bounded live-action outcomes and explicit live-vs-preview control posture. The touched slice still introduced no live secret exposure; the new notices and pulse state derive only from returned action metadata plus refreshed tenant-safe board state, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after typing the live board-action result seam and surfacing the latest bounded action outcome in the page. The touched slice still introduced no live secret exposure; the new result metadata is limited to already-guarded status/run/card/count fields and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the live board page to collect bounded request-field input from harness `requestFields` and derive live action payloads from the contract. The touched slice still introduced no live secret exposure; the new input controls render only tenant-safe field metadata already present in the board contract and do not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after teaching the live board page to keep field defaults, required-field validation, reset behavior, and payload previews contract-driven. The touched slice still introduced no live secret exposure; the new interactivity only derives from tenant-safe request metadata already present in the board contract and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after making preview/live control mode explicit in the board page and client seam, and after collapsing non-recommended option composers behind bounded open/hide controls. The touched slice still introduced no live secret exposure; it only refines tenant-safe UI state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding field-level stale-draft diagnostics plus bounded pruning for removed action keys/request fields on board refresh. The touched slice still introduced no live secret exposure; it only refines tenant-safe UI state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after surfacing bounded contract-refresh notices for pruned stale drafts/fields/composers on live reload. The touched slice still introduced no live secret exposure; it only refines tenant-safe UI state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after enriching contract-refresh notices with action-specific affected-item detail. The touched slice still introduced no live secret exposure; it only refines tenant-safe UI state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after keeping active contract-refresh guidance persistent until dismissal and mirroring that state into the board pulse. The touched slice still introduced no live secret exposure; it only refines tenant-safe UI state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after making prop-seeded preview boards explicitly read-only through the same control-mode seam. The touched slice still introduced no live secret exposure; it only narrows UI mutability and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after widening direct CEO child-lane deferral behavior so owner-conflict and completed-cycle requests now persist bounded deferred governance instead of returning hard conflicts. The touched slice still introduced no live secret exposure; it reuses the existing proposal/decision seam, public bounded status messages, and metadata-only audit fields without widening provider context, required capabilities, or BYOK-bearing runtime state.
- Re-ran the scan after adding contract-aware board recovery feedback plus bounded preview-mode load-failure surfacing. The touched slice still introduced no live secret exposure; the new helpers and page copy carry only bounded error codes, retry windows, action-family labels, and tenant-safe recovery guidance, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding bounded live-board reload controls and automatic stale-action resync for `conflict` / `invalid_request` / `not_found` failures. The touched slice still introduced no live secret exposure; the new recovery seam only reuses the existing tenant-safe board fetch path and action-family metadata, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding bounded retry/reset recovery controls for failed live board actions. The touched slice still introduced no live secret exposure; the new recovery controls only replay the same finite contract payload already entered by the tenant or clear draft state back to contract defaults, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after bounding live board requests with explicit browser-side timeouts. The touched slice still introduced no live secret exposure; the new timeout seam adds only an `AbortSignal`, a bounded `timed_out` client error class, and tenant-safe retry guidance, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after binding live retry/reset controls to the current board contract before replaying a failed action. The touched slice still introduced no live secret exposure; the new guard only narrows when existing tenant-safe control payloads may be replayed and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after distinguishing stale payload drift from stale action disappearance in the live board recovery seam. The touched slice still introduced no live secret exposure; the new payload-compatibility guard only narrows when existing tenant-safe control payloads may be replayed or reset and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding explicit replay-safe / payload-drift / action-removed / board-unavailable recovery states and suppressing reset for replay-safe invalid-request failures. The touched slice still introduced no live secret exposure; the new support-state helpers and feedback metadata only classify existing tenant-safe control payloads and recovery paths, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after catching stale live-board draft drift before submit and falling back to current contract defaults in the preview payload when stored draft values no longer fit allowed values. The touched slice still introduced no live secret exposure; the new drift guard only narrows when tenant-safe control payloads may be submitted and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after preserving last-known contract labels inside contract-refresh guidance and surfacing bounded affected-action plus impact-count metadata in the page and board pulse. The touched slice still introduced no live secret exposure; it only carries tenant-safe action labels, field labels, and count metadata that were already part of the public harness contract, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding bounded next-safe-step recovery guidance to contract-refresh feedback and the board pulse. The touched slice still introduced no live secret exposure; it only adds tenant-safe recovery copy derived from existing public contract state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after splitting top-level CEO follow-on policy into two bounded paths: same-run CEO-originated deferred asks may resolve directly when repeated, while fresh-cycle carried-forward proposals stay in explicit CEO review until approved through the governance seam. The touched slice still introduced no live secret exposure; it reuses existing bounded proposal/decision metadata plus lane ids already present in the governance seam, and it does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding bounded governance-backlog queue-head metadata and backlog composition (`new_work_waiting`, `carry_forward_review`, or `mixed_backlog`) to `pendingAttention`. The touched slice still introduced no live secret exposure; it only surfaces tenant-safe proposal status/count metadata that was already present in the board contract path and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after extending the bounded `actionToken` seam across legacy CEO mutation routes and re-checking latest-cycle exclusivity at commit time. The touched slice still introduced no live secret exposure; it only narrows when existing tenant-safe board mutations may proceed and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after surfacing bounded continuity-memory detail and richer follow-through context from the same persisted board seams. The touched slice still introduced no live secret exposure; it only renders tenant-safe continuity source labels, timestamps, latest outcome summaries, absorbed-work labels, and decision-ledger policy/recommendation metadata that were already present in non-secret harness state, without widening provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding the bounded `memoryBoundary` read model to the board service, fallback client, and board page. The touched slice still introduced no live secret exposure; it only classifies existing tenant-safe board metadata into runtime-memory versus export-ready candidate buckets and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding bounded `memoryBoundary` readiness states (`live_runtime_only`, `ready_now`, `after_board_closes`) across the board service, fallback client, and board page. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-readiness metadata already derived from public board state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` readiness labels, next-eligible export guidance, and pulse-level export summary metadata. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-readiness copy already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` role/source-surface metadata plus partition summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe board/export-boundary semantics already derived from public board state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` candidate-class and durability-condition metadata. The touched slice still introduced no live secret exposure; it only refines tenant-safe long-memory/export-boundary semantics already derived from public board state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` ownership-boundary metadata. The touched slice still introduced no live secret exposure; it only refines tenant-safe runtime-versus-tenant-record ownership posture already derived from public board state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-path metadata. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-readiness path semantics already derived from public board state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` record-target metadata. The touched slice still introduced no live secret exposure; it only refines tenant-safe long-memory target semantics already derived from public board state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-blocker metadata and blocked-candidate summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-blockage semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-authority metadata and authority summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-control semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-trigger metadata and trigger summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-trigger semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-state metadata and state summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-posture semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-next-step metadata and next-step summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe export-next-step semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-action-family metadata and action-family summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export-control-family semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` export-assembly, export-phase, and mutability metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export-shape and long-memory mutability semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` promotion-scope, identity-stability, audit-backing, and concurrency-boundary metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export-safety semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` export-payload-shape, idempotency-policy, replay-safety, and conflict-policy metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export-write semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` export-atomicity metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export commit-granularity semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after adding contract-owned `memoryBoundary` export-derivation, revision, freshness, validation, and completeness metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export preparation semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, or raw execution payloads.
- Re-ran the scan after persisting explicit `bundleRevision` on harness export deliveries and surfacing grouped delivery freshness (`current_bundle` vs `stale_bundle`) through the board/export contract. The touched slice still introduced no live secret exposure; it only tightens tenant-safe delivery freshness and stale-replay suppression semantics around existing export metadata and does not widen provider context, required capabilities, BYOK material, note bodies, or raw execution payloads.
- Re-ran the scan after persisting bounded governance-history snapshots for closed boards and rehydrating the governance export seam from that frozen snapshot. The touched slice still introduced no live secret exposure; it only moves already tenant-safe `recentDecisions` and `followThroughItems` into a harness-owned snapshot ledger and keeps late post-closure runtime noise from rewriting exportable governance history.
- Re-ran the scan after adding contract-owned `memoryBoundary` export-sensitivity, audience-boundary, sanitization-policy, redaction-boundary, and source-disclosure metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export disclosure semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding contract-owned `memoryBoundary` memory-placement, sync-strategy, export-request-shape, export-confirmation, and export-recovery metadata/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe future export/write semantics already derived from public board/package state and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` plus grouped readiness/count summaries. The touched slice still introduced no live secret exposure; it only groups already tenant-safe governance-history and package-output export candidates into first-class later export batches and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` sequence/dependency metadata and grouped graph summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export ordering/dependency semantics for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` write-semantics metadata for payload shape, idempotency, replay safety, conflict policy, and atomicity. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export semantics for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` preparation-semantics metadata for derivation basis, revision policy, freshness source, validation boundary, and completeness rule. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export preparation semantics for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` disclosure-policy metadata for sensitivity, audience boundary, sanitization policy, redaction boundary, and source-disclosure policy. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export disclosure semantics for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` ownership/control metadata for candidate class, durability, ownership boundary, record target, promotion authority, and promotion trigger. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export governance posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` evolution metadata for promotion mutability, scope, identity stability, and audit backing. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export lifecycle posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` concurrency metadata plus grouped concurrency counts/summary. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export safety posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` disclosure counts/summaries for sensitivity, audience, sanitization, and redaction. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export disclosure posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` source-disclosure, request-shape, confirmation, and recovery counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export request/retry posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` placement, sync-strategy, promotion-state, next-step, and action-family counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe later export placement/promotion posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` class, durability, ownership, record-target, and authority counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe grouped governance posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` eligibility-rule, source-surface, promotion-path, blocker, and trigger counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe grouped export evolution flow for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` assembly-shape, phase, mutability, scope, and identity-stability counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe grouped export evolution posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` payload-shape, idempotency, replay-safety, and conflict-policy counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe grouped export write-safety posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` atomicity, derivation-basis, revision-policy, and freshness-source counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe grouped export preparation posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding grouped `memoryBoundary.exportCandidates` validation-boundary and completeness-rule counts/summaries. The touched slice still introduced no live secret exposure; it only refines tenant-safe grouped export readiness posture for already-bounded governance-history and package-output candidates and does not widen provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after adding the shared artifact-id guard and root-bound artifact storage-path resolver. The touched slice still introduced no live secret exposure; it only hardens artifact lookup/staging boundaries by rejecting unsafe ids and requiring future disk-backed resolution to stay under the configured artifact root, without widening provider context, required capabilities, BYOK material, or secret-bearing runtime data.
- Re-ran the scan after adding grouped export preflight, dry-run, and first governance-history export actions. The touched slice still introduced no live secret exposure; it only exposes candidate-owned tenant-safe markdown export bundles and stale-token-protected export contract paths, without widening provider context, required capabilities, BYOK material, raw source payloads, or secret-bearing runtime data.
- Re-ran the scan after promoting governance-history export into an explicit placement-manifest/package-file contract and bounded export audit metadata. The touched slice still introduced no live secret exposure; it adds only tenant-safe vault placement metadata, file checksums/counts, and content-free export audit facts, without widening provider context, required capabilities, BYOK material, raw source payloads into audit logs, or secret-bearing runtime data.
- Re-ran the scan after adding the private governance-history export-ready handoff seam. The touched slice still introduced no live secret exposure; package bodies now travel only through a cloned private runtime hook while public board state, HTTP metadata, queue seams, and audit events remain content-free and tenant-safe.
- Re-ran the scan after adding the harness-owned governance-history export delivery ledger. The touched slice still introduced no live secret exposure; export-ready package bodies now persist only in a private harness-run-scoped delivery table with idempotent metadata and do not widen public board state, generic artifact metadata, provider context, required capabilities, BYOK material, or secret-bearing runtime data.
- Re-ran the scan after adding RLS plus a tenant-member read policy to the harness-owned governance-history export delivery ledger. The touched slice still introduced no live secret exposure; it narrows durable export-record access rather than widening it, and runtime writes still stay on the private harness seam while public board/API/audit surfaces remain content-free.
- Re-ran the scan after surfacing governance-history delivery posture back through the grouped export-candidate contract and board read model. The touched slice still introduced no live secret exposure; it only reflects bounded delivery status, attempt counts, writer labels, safe note paths, and last-error summaries from the private harness ledger without replaying note bodies, provider context, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after adding bounded governance-history delivery replay through the persisted export-ready bundle seam. The touched slice still introduced no live secret exposure; replay reuses only the private harness delivery ledger plus the existing private writer hook, and it does not widen note bodies, provider context, BYOK material, or other secret-bearing runtime data onto public board, HTTP, or audit surfaces.
- Re-ran the scan after widening the real export seam to include `package_bundle_export` on the same bounded preflight/dry-run/export/replay rails. The touched slice still introduced no live secret exposure; package bundle bodies now follow the same private harness delivery ledger and private writer hooks as governance history, while public board state, HTTP metadata, and audit events remain content-free and tenant-safe.
- Re-ran the scan after persisting closed-board `completionPackage` snapshots. The new ledger stores only bounded tenant-facing package summaries, governance items, and deliverables; it does not add raw private note bodies, vault roots, or backend runtime context to any public or cross-tenant surface.
- Re-ran the scan after adding compare-and-set `delivery_in_progress` claims to the harness export-delivery seam. The touched slice still introduced no live secret exposure; it only hardens duplicate-writer suppression and truthful replay posture around already private tenant-safe bundles, without widening note bodies, provider context, BYOK material, or secret-bearing runtime data onto public board, HTTP, or audit surfaces.
- Re-ran the scan after making export bundle identity revision-aware and failing closed on stale replay. The touched slice still introduced no live secret exposure; it only tightens tenant-safe delivery freshness by minting new bundle/idempotency identities for changed export content and by refusing to replay older stored bundles once the current export contract has moved on.
- Re-ran the scan after unifying runtime export delivery and widening bounded writer receipts with relative written-path truth. The touched slice still introduced no live secret exposure; it only keeps governance-history and package-bundle claim/outcome handling on one shared private path while exposing recovery-safe relative filesystem progress instead of vault roots, note bodies, provider context, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after adding package-bundle stale-claim interleaving proof and tightening `deliveryReceipt` into a bounded typed contract. The touched slice still introduced no live secret exposure; it only proves the existing private recovery path on the second export family and narrows receipt consumers to safe relative-path/count metadata instead of widening vault roots, note bodies, provider context, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after widening the private worker dispatch handoff with explicit execution-stage metadata and follow-on run-reactivation truth. The touched slice still introduced no live secret exposure; it only refines lane-safe orchestration metadata for private runtime consumers and does not widen provider context, note bodies, BYOK material, or secret-bearing execution payloads onto public dispatch telemetry.
- Re-ran the scan after adding explicit execution-claim tokens to `working` harness lanes and private execution envelopes. The touched slice still introduced no live secret exposure; it only hardens stale-worker rejection and already-working claim refresh on the private lane-execution seam, without widening provider context, note bodies, BYOK material, or secret-bearing runtime data onto public dispatch or outcome telemetry.
- Re-ran the scan after adding bounded `execution_claimed` and `execution_claim_refreshed` lifecycle events. The touched slice still introduced no live secret exposure; the new events persist only metadata-safe claim lifecycle timestamps and claim-kind labels, not raw claim tokens, provider context, note bodies, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after adding persona-focus lane-cap governance to both direct CEO requests and proposal approvals. The touched slice still introduced no live secret exposure; it only widens bounded policy metadata (`persona_lane_cap`, recommendation/objection summaries, and next-review triggers) and does not widen provider context, note bodies, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after aligning worker/runtime verification with the explicit execution-claim envelope shape. The touched slice still introduced no live secret exposure; it only updates tests around private claim metadata (`kind`, `previousClaimedAt`) and keeps those fields on the private execution seam rather than public dispatch telemetry.
- Re-ran the scan after adding structured private continuity context to execution envelopes. The touched slice still introduced no live secret exposure; the new absorbed-work trail, latest absorbed item, continuity summary, and result memory stay on the private worker handoff and are explicitly absent from public lane-dispatch telemetry, provider context, BYOK material, and other secret-bearing runtime surfaces.
- Re-ran the scan after adding bounded ignored-outcome handoffs for stale-worker and non-working-lane callbacks. The touched slice still introduced no live secret exposure; the new runtime seam exposes only metadata-safe ignored reason, lane-state, and claim-presence/claimed-at truth, and it explicitly avoids replaying raw execution-claim tokens, provider context, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after carrying private claim provenance alongside the public harness dispatch seam. The touched slice still introduced no live secret exposure; the new runtime-only claim context keeps `approved_claim` versus refreshed/existing working classification deterministic for private execution envelopes without widening public dispatch telemetry or exposing raw claim tokens.
- Re-ran the scan after persisting bounded `execution_outcome_ignored` events and surfacing them in board history. The touched slice still introduced no live secret exposure; the new durable support trace records only metadata-safe ignored reasons and lane-state/claim-presence timing truth, not raw execution-claim tokens, provider context, BYOK material, or other secret-bearing runtime data.
- Re-ran the scan after extending that ignored-outcome durability to post-closure terminal-run callbacks. The touched slice still introduced no live secret exposure; the new support trace adds only metadata-safe terminal run-state truth on the target child lane and still avoids raw execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after persisting bounded `execution_dispatched` events for initial claims and follow-on dispatches. The touched slice still introduced no live secret exposure; the new history records only metadata-safe handoff truth (`initial_claim` versus `follow_on_dispatch`, bounded execution stage, reactivation posture, and triggering persona/outcome summary when present) and avoids raw execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after persisting bounded `execution_outcome_committed` events for successful worker outcomes. The touched slice still introduced no live secret exposure; the new history records only metadata-safe outcome state, run state, bounded next-step posture, and already-sanitized result/continuity summaries, and it avoids raw execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after adding a dedicated private committed-outcome runtime handoff. The touched slice still introduced no live secret exposure; the new `wealth_factory_harness_lane_outcome_committed` event and optional hook carry only the already-bounded lane execution snapshot, attention transition metadata, bounded post-outcome action truth, and optional next-dispatch metadata, without widening raw execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after splitting committed outcomes into state-specific private runtime handoffs. The touched slice still introduced no live secret exposure; the new `wealth_factory_harness_lane_done`, `..._waiting`, `..._blocked`, and `..._cancelled` events/hooks only partition the same bounded committed-outcome metadata and do not widen raw execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after splitting ignored callbacks into state-specific private runtime handoffs. The touched slice still introduced no live secret exposure; the new `wealth_factory_harness_lane_outcome_ignored_stale_claim`, `..._lane_not_working`, and `..._terminal_run` events/hooks only partition the same bounded ignored-outcome metadata and do not widen raw execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after tightening tenant-facing committed-outcome history copy. The touched slice still introduced no live secret exposure; the new activity labels only reuse already-bounded continuity summaries and follow-on persona metadata from durable worker outcome events, and they do not widen execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after tightening tenant-facing dispatch history copy. The touched slice still introduced no live secret exposure; the new activity labels only reuse already-bounded dispatch-stage, reactivation, and triggering-persona metadata from durable worker dispatch events, and they do not widen execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.
- Re-ran the scan after tightening tenant-facing claim history copy. The touched slice still introduced no live secret exposure; the new activity labels only reuse already-bounded claim-kind metadata from durable worker claim events, and they do not widen execution-claim tokens, provider context, BYOK material, note bodies, or other secret-bearing runtime data.

## Remediation Priority

1. Maintain ignore/exclude hygiene for `.env` and local operational artifacts so the clean harness slice cannot be contaminated by deployment-time staging mistakes.
2. Keep the harness `runtime_context` sanitization and response-guard tests in the full gate to prevent future regressions that reintroduce `secretValues` or customer-facing execution noise.
3. Keep tenant-authored summaries on parsed body surfaces only, and extend the same metadata-only audit discipline if broader harness mutation paths begin recording richer business outcomes.
4. Keep the new runtime-memory shape metadata export-neutral: the explicit continuity-trio and live-attention descriptors should stay operational contract truth and must not be treated as a new tenant-export surface without separate disclosure review.
5. Keep the new dispatch handoff, private claim-provenance, execution-claim, ignored-outcome, execution-lifecycle-event, structured continuity, and persona-focus governance contracts metadata-only: initial-claim versus follow-on context, private claim-origin classification, ignored stale-callback reason metadata, claim lifecycle timestamps, parsed absorbed-work lineage, and policy summaries like `persona_lane_cap` can help private runtime consumers and board reviewers reject stale workers or over-committed personas safely, but those seams must stay free of raw execution-claim tokens, provider context, raw note bodies, or any content that would widen public lane-dispatch telemetry.

## Conclusion

The current Wealth Factory harness slice is in a good security position: no confirmed live secret exposure was introduced, no tenant secret values are persisted in the new harness records, the board API remains customer-safe and high-level, run reconciliation now operates on persisted child-card/proposal state, completion is explicit and CEO-gated, proposal policy can now defer/deny or reuse an existing lane while keeping deferred work visible and reviewable, the decision ledger now carries bounded governance memory without replaying raw CEO notes, and the widened worker seam now requires both a durable claim and bounded persisted start-state updates before any public `running` signal is emitted while also keeping private lane outcomes inside a compare-and-set, non-secret-bearing mutation path. The new bounded follow-on dispatch path also stays lane-safe: it can only advance one next `approved` lane at a time, records the guarded workflow status that matches that claim, and still carries no BYOK or provider-secret material. The new post-outcome orchestrator decision seam stays equally bounded: it adds only finite control metadata about whether the engine should dispatch, queue CEO review, await resume, or await unblock, not any new credential-bearing or tenant-secret fields. The new runtime-consumption seam preserves that boundary by emitting the same bounded follow-through truth through a dedicated non-dispatch event/hook instead of re-deriving it from broader runtime state, the explicit runtime-side handlers keep that handoff partitioned without widening telemetry or provider-context surfaces, the new failure-isolation split keeps one rejected runtime consumer from suppressing the rest of the already-committed handoff path, and the new specific worker events mirror those same bounded control paths without introducing any new sensitive payload surface. The new shared post-outcome classifier, persisted `attention_requested` / `attention_resolved` events, explicit worker `attentionTransition` metadata, and board-facing `pendingAttention` read model stay inside that same bounded seam: they surface only control metadata plus continuity-backed or previously persisted tenant-safe summaries, not provider context, required capabilities, or secret-bearing runtime data, and they now resolve prior attention explicitly instead of piling up unresolved board noise or re-deriving attention text from mutable lane state later. The same-lane refinement reopen seam is also bounded on both approval paths, and the normalized title matcher stays narrow to bounded assignment text rather than any secret-bearing payload. The remaining work is operational hygiene and eventually moving audit publishing from the current best-effort post-commit path into a stronger guaranteed persistence seam, not an immediate security blocker for continued development.
